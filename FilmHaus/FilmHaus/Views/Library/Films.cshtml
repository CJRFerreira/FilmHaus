@using FilmHaus.Localization
@model FilmHaus.Models.View.FilmLibraryViewModel
@{
    ViewBag.Title = Locale.MyFilms;
}

<h2>@Locale.MyFilms</h2>
<hr />
@if (Model.Films.Count > 0)
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th></th>
                <th></th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Films)
                {
                <tr>
                    <td>@item.PosterUri</td>
                    <td>@item.FilmName</td>
                    <td>@item.DateOfRelease</td>
                    <td>@item.Accolades</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="well">
        <p class="text-info">
        </p>
    </div>
}