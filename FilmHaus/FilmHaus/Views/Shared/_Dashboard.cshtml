@using FilmHaus.Localization;
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - @Locale.FilmHaus</title>
    @Styles.Render("~/Content/main/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div id="wrapper">
        <div class="overlay"></div>

        <!-- Sidebar -->
        <nav class="navbar navbar-inverse navbar-fixed-top" id="sidebar-wrapper" role="navigation">
            <ul class="nav sidebar-nav">
                <li class="sidebar-brand">
                    <a href="#">@Locale.FilmHaus</a>
                </li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">@Locale.Hello @User.Identity.Name<span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li class="dropdown-header">@Locale.UserActions</li>
                        <li>@Html.ActionLink(Locale.Manage, "Index", "Manage", routeValues: null, htmlAttributes: new { title = Locale.Manage })</li>
                        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
                        {
                            @Html.AntiForgeryToken()
                            <li><a href="javascript:document.getElementById('logoutForm').submit()">@Locale.LogOff</a></li>
                        }
                    </ul>
                </li>
                <li>
                    @Html.ActionLink(Locale.MyFilms, "Index", "Film")
                </li>
                <li>
                    @Html.ActionLink(Locale.MyShows, "Index", "Series")
                </li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">@Locale.MyLists <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li class="dropdown-header">@Locale.UserActions</li>
                        <li>@Html.ActionLink(Locale.ViewLists, "Index", "List")</li>
                        <li>@Html.ActionLink(Locale.CreateList, "CreateList", "List")</li>
                    </ul>
                </li>
                <li>
                    @Html.ActionLink(Locale.MyReviews, "Index", "Review")
                </li>
                <li>
                    @Html.ActionLink(Locale.MyReports, "Index", "Report")
                </li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">@Locale.Search <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li class="dropdown-header">@Locale.UserActions</li>
                        <li>@Html.ActionLink(Locale.SearchFilms, "SearchFilms", "Search")</li>
                        <li>@Html.ActionLink(Locale.SearchShows, "SearchShows", "Search")</li>
                        <li>@Html.ActionLink(Locale.SearchLists, "SearchLists", "Search")</li>
                        <li>@Html.ActionLink(Locale.SearchReviews, "SearchReviews", "Search")</li>
                    </ul>
                </li>
            </ul>
        </nav>
        <!-- /#sidebar-wrapper -->
        <!-- Page Content -->
        <div id="page-content-wrapper">
            <button type="button" class="hamburger is-closed" data-toggle="offcanvas">
                <span class="hamb-top"></span>
                <span class="hamb-middle"></span>
                <span class="hamb-bottom"></span>
            </button>
            <div class="container">
                <div class="row">
                    <div class="col-lg-8 col-lg-offset-2">
                        @RenderBody()
                    </div>
                </div>
            </div>
        </div>
        <!-- /#page-content-wrapper -->
    </div>
    <!-- /#wrapper -->
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
    <script>
        $(document).ready(function () {
            var trigger = $('.hamburger'),
                overlay = $('.overlay'),
               isClosed = false;

            trigger.click(function () {
                hamburger_cross();
            });

            function hamburger_cross() {

                if (isClosed == true) {
                    overlay.hide();
                    trigger.removeClass('is-open');
                    trigger.addClass('is-closed');
                    isClosed = false;
                } else {
                    overlay.show();
                    trigger.removeClass('is-closed');
                    trigger.addClass('is-open');
                    isClosed = true;
                }
            }

            $('[data-toggle="offcanvas"]').click(function () {
                $('#wrapper').toggleClass('toggled');
            });
        });
    </script>
</body>
</html>